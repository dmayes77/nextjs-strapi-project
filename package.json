{
    "name": "fullstack-app",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run develop --prefix backend\""
    },
    "dependencies": {
        "concurrently": "^9.1.2"
    }
}
